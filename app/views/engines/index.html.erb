<div class="row">
    <div class="col-md-3">
        <ul class="sidebar-list">
           <li class="sidebar-item"><%= link_to "Mes publications", engines_path, class: "sidebar-link active" %></li>
        </ul>
    </div>
       <div class="col-md-9">
            <div class="panel panel-default">
                <div class="panel-heading">Mes publications</div>
                    <div class="panel-body">
                        <% @engines.each do |engine| %>
                            <div class="row">
                                <div class="col-md-2">
                                    <%= image_tag engine.photos[0].image.url(:thumb) if engine.photos.length > 0 %> 
                                </div>                              
                                <div class="col-md-7">
                                    <h4><%= engine.engine_name %></h4>
                                </div>                              
                                <div class="col-md-3 right">
                                    <%= link_to "Mettre à jour", edit_engine_path(engine), class: "btn btn-primary" %>
                                </div>
                            </div>
                        <hr>
                        <% end %>
                    </div>
                </div>
            </div>
        </div>